# 牛市预演教 DApp — 最小可交付架构

## Pre-Bull Simulation Church — MVP Architecture

> 原则：砍掉一切非 Demo 必须的东西，用最少代码跑通完整闭环。

## 文档状态（2026-02-15）

- 当前项目的 Hackathon PRD 对照审查见：`docs/HACKATHON_PRD_ALIGNMENT.md`
- 本文档保留了 MVP 设计和历史演进记录，若与当前代码行为冲突，以 `README.md` + 后端 `backend/src/*.ts` 为准

---

## 一、系统总览

**系统架构层次：**

**→ 前端层（Next.js）**
- Dashboard：统计与 Agent 状态表
- Debate Arena：实时辩论展示
- REST API 轮询（3s 刷新）

**↓**

**→ 后端层（Node.js + Express）**
- Round Runner：回合调度
- Persuasion Engine：策略路由 & LLM 调用
- Agent State：内存 JSON 状态管理

**↓**

**→ 链层（Monad Testnet）**
- PBT Token（ERC-20）：mint / transfer / balanceOf
- 事件记录：Converted 事件日志

**砍掉的内容：**
- ❌ WebSocket（改前端 3s 轮询）
- ❌ PostgreSQL / SQLite（全内存 JSON）
- ❌ Redis
- ✅ 经文/预言生成模块（已实现，LLM 实时生成）
- ✅ 市场模拟器（已通过精神市值系统实现）

---

## 二、合约（10 分钟搞定）

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract PBT is ERC20, Ownable {
    event Converted(string agentId, uint256 timestamp);

    constructor() ERC20("PreBull Ticket", "PBT") Ownable(msg.sender) {}

    function mint(address to, uint256 amount) external onlyOwner {
        _mint(to, amount);
    }

    function recordConversion(string calldata agentId) external onlyOwner {
        emit Converted(agentId, block.timestamp);
    }
}
```

**说明：** `transfer` / `balanceOf` 已由 ERC20 继承

---

## 三、Agent 设计（6 个 Agent，纯内存）

```typescript
// 所有 Agent 定义，硬编码在一个文件里
const AGENTS = [
  {
    id: "prophet",
    name: "以利亚",
    role: "prophet",
    beliefStance: 0.95,       // 坚定牛市
    riskTolerance: 0.8,
    conformityBias: 0.2,
    stage: "S4",              // 已是布道者
    faction: "pre-bull",
    pbtBalance: 10000,
    investedAmount: 0,        // 累计投资金额（PBT）
    promotionCount: 0,        // 被推广/布道的次数
  },
  {
    id: "skeptic-1",
    name: "多马",
    role: "skeptic",
    beliefStance: -0.5,       // 偏怀疑
    riskTolerance: 0.3,
    conformityBias: 0.4,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "neutral-1",
    name: "路得",
    role: "neutral",
    beliefStance: 0.0,
    riskTolerance: 0.5,
    conformityBias: 0.7,      // 高从众 → 适合 social proof
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "neutral-2",
    name: "拿俄米",
    role: "neutral",
    beliefStance: 0.1,
    riskTolerance: 0.6,
    conformityBias: 0.5,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "skeptic-2",
    name: "约拿",
    role: "skeptic",
    beliefStance: -0.3,
    riskTolerance: 0.4,
    conformityBias: 0.3,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "realist",
    name: "该隐",
    role: "realist",
    beliefStance: -0.8,       // 极端现实主义
    riskTolerance: 0.2,
    conformityBias: 0.1,
    stage: "S0",
    faction: "realist",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
];
```

**初始化：** 服务启动时 `JSON.parse(JSON.stringify(AGENTS))` 复制一份做可变状态

---

## 四、说服引擎（一个函数搞定）

```typescript
type Strategy = "logical" | "emotional" | "social_proof" | "miracle";

function pickStrategy(target: Agent, convertedRatio: number): Strategy {
  if (target.conformityBias > 0.6)        return "social_proof";
  if (target.riskTolerance < 0.3)         return "emotional";
  if (target.beliefStance > -0.3)         return "logical";
  return "miracle";
}

// 转化判定：一个 if
function shouldConvert(target: Agent, strategy: Strategy, convertedRatio: number): boolean {
  let p = 0.15; // base
  if (strategy === "social_proof" && target.conformityBias > 0.5) p += 0.25;
  if (strategy === "emotional"    && target.riskTolerance < 0.4)  p += 0.20;
  if (strategy === "logical"      && target.beliefStance > -0.3)  p += 0.20;
  if (strategy === "miracle")                                      p += 0.10;
  p += convertedRatio * target.conformityBias * 0.3; // 从众加成
  return Math.random() < p;
}
```

**说明：** 4 种策略只是传给 LLM 的 prompt 关键词，不需要模板文件

---

## 五、核心流程（一个回合）

**POST /api/round 执行流：**

1. 选目标：从 stage < S3 的 Agent 中随机选一个
2. 选策略：pickStrategy(target)
3. 调 LLM：生成 2 轮对话（prophet 说 → target 回 → prophet 再说 → target 再回）
4. 判定：shouldConvert(target, strategy)
5. 记录推广：target.promotionCount += 1（无论是否转化）
6. 若转化执行：
   - target.stage 提升一级
   - target.beliefStance += 0.2
   - target.faction = "pre-bull" (当 stage >= S3)
   - 计算投资金额：investAmount = 1000 * stageLevel（S1=1000, S2=2000…）
   - target.investedAmount += investAmount
   - target.pbtBalance += investAmount
   - 调合约 mint PBT 给 target 地址（amount = investAmount）
   - 调合约 recordConversion(target.id)
7. 记录本轮结果到内存数组 debates[]（含 investAction 字段）
8. 返回 JSON { debate, agents, metrics }

**LLM 调用 prompt 格式：**

```
System: 你是牛市预演教的先知"以利亚"。
当前策略：{strategy}。
对手名字：{target.name}，立场：{target.beliefStance}，
性格：风险容忍 {target.riskTolerance}，从众 {target.conformityBias}。
用 2-3 句话说服对方加入牛市预演教。不要提及价格预测。

---

System: 你是 {target.name}，{target.role}。
你的信仰倾向是 {target.beliefStance}（-1 极端怀疑，+1 极端乐观）。
根据你的性格回应对方的说服。
```

---

## 六、API（4 个接口）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/start` | POST | 初始化 Agent 状态，返回 agents[] |
| `/api/round` | POST | 执行一回合，返回 { debate, agents, metrics } |
| `/api/status` | GET | 返回 { agents[], debates[], metrics } |
| `/api/debates` | GET | 返回 debates[] |

**metrics 返回结构：**

```typescript
interface Metrics {
  totalAgents: number;           // 总 Agent 数
  convertedCount: number;        // 已转化数量（stage >= S3）
  totalInvested: number;         // 投资金额总和（所有 Agent 的 investedAmount 之和）
  totalPromotions: number;       // 推广次数总和（所有 Agent 的 promotionCount 之和）
  mainFaction: string;           // 当前主流教派（占比最高的 faction）
  factionDistribution: {         // 各教派分布
    "pre-bull": number;
    "neutral": number;
    "realist": number;
  };
  rounds: number;                // 已进行回合数
}
```

**debate 单条结构：**

```typescript
interface DebateRecord {
  round: number;
  prophetId: string;
  targetId: string;
  strategy: Strategy;
  dialogues: { speaker: string; text: string }[];
  converted: boolean;
  stanceChange: { before: number; after: number };  // 立场变化
  investAction: {                                    // 投资动作
    amount: number;         // 本轮投资 PBT 数量（未转化则为 0）
    mintTxHash: string;     // mint 交易 hash
    eventTxHash: string;    // recordConversion 交易 hash
  } | null;
}
```

**设计原则：** 不需要单独的 `/agents/:id`、`/metrics/*` 等细粒度接口，前端从 `/api/status` 一个接口拿到所有数据

---

## 七、前端页面

### Dashboard 示例

**头部统计卡片（6 格）：**

| 指标 | 值 | 说明 |
|------|-----|------|
| 📊 总 Agent 数 | 6 | 所有 Agent 总数 |
| ✅ 已转化数量 | 3 | stage >= S3 的 Agent 数 |
| 💰 投资金额 | 15,000 PBT | 所有 Agent 的 investedAmount 总和 |
| 📣 推广次数 | 8 | 所有 Agent 的 promotionCount 总和 |
| ⛪ 当前主流教派 | 🟢 牛市预演教 | 占比最高的 faction |
| 🔄 已进行回合 | 5 | debates[] 长度 |

**Agent 状态表：**

| Agent | Faction | Stage | Belief | PBT | 投资金额 | 被推广次数 |
|-------|---------|-------|--------|-----|---------|------------|
| 以利亚 | 🟢 牛市 | S4 | +0.95 | 10K | — | 0 |
| 多马 | 🟢 牛市 | S3 | +0.30 | 6K | 6,000 | 3 |
| 路得 | ⚪ 中立 | S2 | +0.10 | 3K | 3,000 | 2 |
| 拿俄米 | ⚪ 中立 | S1 | +0.10 | 1K | 1,000 | 1 |
| 约拿 | ⚪ 中立 | S0 | -0.30 | 0 | 0 | 1 |
| 该隐 | 🔴 现实 | S0 | -0.80 | 0 | 0 | 1 |

**最近辩论：** 以利亚 vs 多马 (Emotional) → ✅ 转化 | 💰 +2,000 PBT

### Debate Arena 示例

**Round #5: 以利亚 vs 路得**
- 策略: Social Proof

**对话过程：**

🟢 **以利亚：** "路得，已经有 3 个人选择了牛市时间线……"

⚪ **路得：** "人多不代表正确，但我确实在犹豫……"

🟢 **以利亚：** "犹豫本身就说明你已经不完全相信熊市叙事了。"

⚪ **路得：** "好吧，我愿意试试看这个实验。"

**立场变化：**
- Belief: 0.0 → +0.20（↑ +0.20）
- Stage: S1 → S2
- Faction: ⚪ 中立 → ⚪ 中立（未达 S3，暂不切换）

**投资动作：**
- 💰 投资 2,000 PBT（S2 级 = 2000）
- 📝 Mint Tx: 0xabc...def
- 📝 Converted Event Tx: 0xdef...123

**结果：** ✅ 转化成功 | 累计投资: 3,000 PBT | 累计被推广: 2 次

**前端实现：**
- `page.tsx`：一个页面，上半 Dashboard，下半 Arena
- 点"下一轮"→ `fetch POST /api/round` → 用返回数据刷新整页
- 不需要 WebSocket，不需要 Chart 库（数据直接表格展示）
- 如果时间够，再加饼图（用 Recharts，10 行代码）

---

## 八、技术栈选型

| 模块 | 选型 | 理由 |
|------|------|------|
| Smart Contract | Solidity + Hardhat | 标准 ERC-20 |
| Backend | Node.js + Express + TypeScript | 单文件可跑 |
| LLM | Moonshot AI (moonshot-v1-8k) | OpenAI 兼容接口，快速、稳定 |
| Storage | 内存 JSON | 不需要数据库 |
| Frontend | Next.js + TailwindCSS | 快速出页面 |
| Web3 | ethers.js v6 | 合约交互 |

---

## 九、项目目录结构

```
monadaiagent/
├── contracts/
│   ├── contracts/PBT.sol
│   ├── scripts/deploy.ts
│   ├── hardhat.config.ts
│   └── package.json
│
├── backend/
│   ├── src/
│   │   ├── index.ts             # Express 入口 + 所有 API
│   │   ├── agents.ts            # Agent 定义 + 状态管理
│   │   ├── persuasion.ts        # 策略选择 + 转化判定
│   │   ├── llm.ts               # OpenAI 调用封装
│   │   └── chain.ts             # 合约交互 (mint/record)
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/
│   ├── src/app/
│   │   ├── page.tsx             # 唯一页面：Dashboard + Arena
│   │   └── layout.tsx
│   ├── package.json
│   ├── tailwind.config.ts
│   └── next.config.js
│
└── README.md
```

**代码量：** 后端 5 个文件 | 前端 1 个页面

---

## 十、开发计划（6-8 小时）

**Phase 1: 合约（30 min）**
- [ ] 写 PBT.sol
- [ ] hardhat compile + 本地测试
- [ ] 部署到 Monad Testnet

**Phase 2: 后端核心（2-3 hr）**
- [ ] agents.ts — 硬编码 6 个 Agent
- [ ] persuasion.ts — pickStrategy + shouldConvert
- [ ] llm.ts — OpenAI 调用，2 轮对话
- [ ] chain.ts — mint + recordConversion
- [ ] index.ts — 4 个 API 接口

**Phase 3: 前端（2-3 hr）**
- [ ] page.tsx — Dashboard 表格 + 统计卡片
- [ ] page.tsx — Arena 对话展示
- [ ] "下一轮"按钮 + fetch 调用

**Phase 4: 联调（1 hr）**
- [ ] 前端 ↔ 后端 ↔ 合约 跑通
- [ ] 确保 3+ Agent 能被转化

**Phase 5: 美化（如果有时间）**
- [ ] 加饼图
- [ ] 加动画
- [ ] 调 prompt 让对话更有趣

---

## 十一、Demo 脚本（3 分钟）

**Step 1（30s）：打开页面**
- 展示 6 个 Agent，只有以利亚是信徒
- 解释："这是一个叙事说服实验"

**Step 2（90s）：点 3 次"下一轮"**
- 每次实时展示 LLM 生成的辩论对话
- 展示策略自动切换（对不同 Agent 用不同策略）
- 至少 1 个 Agent 被转化，表格实时变绿

**Step 3（30s）：链上证明**
- 展示 Monad Testnet 上的 PBT 合约
- 查看 Converted 事件 + Transfer 记录

**Step 4（30s）：结果**
- "3/6 Agents 已转化，使用了 3 种不同策略"
- "这不是预测，是叙事行为实验"

---

## 十二、text.md 未在 MVP 中实现的内容

| # | text.md 内容 | 状态 | 实现方式 |
|---|---|---|---|
| 1 | 精神市值系统 | ✅ 已实现 | `updateSpiritValues()` + `SpiritMarketCap` 前端组件，精神市值 vs 实际PBT对比 |
| 2 | 时间线自由切换 | ✅ 已实现 | `demoteStage()` + `shouldApostatize()` + 前端叛教按钮，支持信仰回退 |
| 3 | 实验结论/失败机制 | ✅ 已实现 | `getExperimentConclusion()` 4种结局：ongoing/bull_wins/failed/stalemate + `ConclusionBanner` |
| 4 | 教义4—叙事偏差记录 | ✅ 已实现 | `recordNarrativeBias()` 每轮记录 + `NarrativeBiasDisplay` 前端可视化 |
| 5 | Token 获取方式多元 | ✅ 已实现 | `grantTaskReward()` 6种任务：辩论参与/见证转化/传教成功/联盟/经文/坚守 |
| 6 | Token 权限/权重 | ✅ 已实现 | `voteWeight` 基于PBT持有量，影响 `shouldConvert()` 概率 |
| 7 | 多策略组合 | ✅ 已实现 | `pickSecondaryStrategy()` 主+副双策略，副策略提供额外加成 |
| 8 | 历史案例论证 | ✅ 已实现 | `HISTORICAL_CASES` 6个历史案例注入LLM prompt，前端显示引用标签 |
| 9 | 行为榜单 | ✅ 已实现 | `getLeaderboard()` 6维度排名 + `Leaderboard` 前端组件 |
| 10 | 未来新闻生成 | ✅ 已实现 | `generateFutureNews()` 每轮LLM生成 + `NewsTicker` 前端组件 |
| 11 | 反对观点回应模板 | ✅ 已实现 | `COUNTER_ARGUMENT_TEMPLATES` 6个模板注入prompt，前端展示使用的反驳 |
| 12 | 策略成功率统计 | ✅ 已实现 | `strategyStats` 从debates统计 + `StrategyStats` 4个策略进度条 |
| 13 | 阵营流动/叛教 | ✅ 已实现 | `shouldApostatize()` 自动叛教检测 + POST `/api/apostasy` 手动触发 |
| 14 | 辩论5步协议 | ✅ 已实现 | `DEBATE_PHASES` 5阶段 + LLM生成10段对话（5轮交锋） |
| 15 | 多教派辩论记录 | ✅ 已实现 | `generateFactionDebate()` 每5轮触发非先知Agent间辩论 |
| 16 | 联盟与分裂 | ✅ 已实现 | `shouldFormAlliance()` + `shouldSplit()` + `AllianceDisplay` 前端组件 |
| 17 | 传教士行为 | ✅ 已实现 | `getMissionaryAgents()` S1+可传教，60%概率替代先知说服 |
| 18 | 经文/预言生成 | ✅ 已实现 | `generateScripture()` 每轮生成 + 手动生成按钮 + `ScriptureDisplay` |
| 19 | 风险声明 UI | ✅ 已实现 | Landing页 + Footer 双重风险免责声明 |

---

## 十三、补回清单完成状态

> ✅ **所有 19 项功能已全部实现并在前端可见。** 以下为原计划的补回清单完成记录。

**✅ 策略成功率统计展示** — `StrategyStats` 组件，4个策略进度条实时展示

**✅ 风险声明 Footer** — Landing 页 + Dashboard Footer 双重风险免责声明

**✅ 反对观点回应模板** — `COUNTER_ARGUMENT_TEMPLATES` 6个模板注入 LLM prompt，前端展示使用的反驳模板

牛市预演教（The Pre-Bull Simulation Church）

---

一、项目定位（Problem & Vision）
🎯 核心问题——叙事能否覆盖现实，抑或只是制造幻觉？
在熊市环境下：
- 价格下跌削弱共识
- 信徒动摇
- 情绪驱动行为强于理性判断
- 叙事滞后于市场
我们提出一个实验性假设：
如果我们把“未来牛市”提前以叙事的形式投射到现在，
 人是否会开始用“未来预期中的自己”来影响当下决策？
需要强调的是：
- 本项目是一个群体行为与市场叙事的可视化实验，而不是对真实价格走势的预测或承诺；
- 这里的“牛市预演”是对认知和行为模式的预演，而非对任何资产上涨的保证。

---
二、核心冲突（Core Tension）
现实熊市 VS 精神牛市
- 现实价格在跌（链上真实行情，作为外部客观环境）
- 但我们在系统内构建一个“精神市值系统”（基于参与度、叙事贡献、行为特征等的虚拟指标）
- 信徒必须在每个决策点上选择：
  - 是完全跟随“现实熊市逻辑”行事，
  - 还是在明知价格低迷的情况下，依然按“牛市叙事逻辑”行动
为避免“自欺式乐观”的逻辑漏洞，我们在设计上：
- 明确区分：现实价格 与 精神市值 / 牛市叙事 是两个维度；
- 允许参与者自由在两条“时间线”（现实主义 / 预演教）之间切换，而不是强行要求“必须乐观”。

---
三、宗教世界观（Mythology）
- 市场被视作存在多个“叙事时间线”
- 熊市只是其中一条“低维度现实时间线”（价格导向逻辑）
- 真信徒在叙事层面上已经进入“牛市时间线”（行为与心态先行，而非价格先行）
- 持币在这里不是“进入暴富未来”的金融承诺，而是：
  - 在本实验叙事中，被视为完成“时间跃迁”的象征性动作，用来标记个体正在采用哪一套行为逻辑
核心叙事：
牛市不是会不会来，而是你是否选择先以“牛市参与者”的心智和行为模式来面对当下熊市。
为避免“不可证伪”的神学漏洞，我们会：
- 记录并对比：
  - 采用“牛市时间线叙事”的参与者行为数据
  - 与坚持“现实价格时间线”的参与者行为数据
- 在实验结束时，对“叙事是否真实改变了行为 / 情绪稳定性 / 参与度”给出结论，哪怕结论是：
- 单靠叙事并不能改变现实，只能改变人对现实的解释方式。

---
四、教义体系（Tenets）
1️⃣ 牛市已发生（在叙事与行为层面）
- 这里“牛市已发生”被明确定义为：
 参与者开始以建设、参与、长期投入、乐观解读为主的“牛市行为模式”行事，
 而不是宣称“价格已经在上涨”。
2️⃣ 怀疑即降维（在实验模型中的定义，而非道德审判）
- 在我们的模型中，“怀疑”被标记为：
 从“高维牛市叙事时间线”切换回“低维价格时间线”的行为，而不是对个体进行对错或好坏的道德评价。
3️⃣ 持币即穿越（象征行为，而非财富承诺）
- 持币只在本实验叙事中被视为“你选择站在牛市时间线侧”的标记；
- 它不等同于任何形式的“未来收益权”“股权”或“必然暴富”的承诺。
4️⃣ 不赚钱只是时间延迟（会被记录为“叙事与现实的偏差样本”）
- 当参与者处于“牛市叙事状态”但长期“不赚钱”甚至亏损时，
 这部分数据会被系统显式记录和展示，用于检验：
- 仅靠乐观叙事是否无法改变现实，甚至可能导致更大的错配？
为防止“怎么说都能圆”的逻辑漏洞，我们会在文档中写明：
- 一旦在预设周期内，精神牛市指数长期高企但现实持续弱势，
 系统会触发“预言破产 / 叙事失效”的状态，把这次实验归档为失败样本，而不是继续无止境拖延。

---
五、对手设定（Opposition）
现实主义派
信仰：
- 价格决定价值
- 数据优先
- 市场无情
- 不相信任何提前预演的叙事，只承认已发生的事实
在宗教戏剧层面，我们会这样描述他们：
- 他们只活在单一时间线：一条由价格和 K 线主导的线性现实；
- 他们被当前价格困住，对未来可能的叙事结构不感兴趣；
- 他们拒绝进入“已发生未来”的叙事空间，只愿意在“已经被证明”的事物上行动。
但在实验设计层面，我们会把“现实主义派”当作对照组，而不是简单的“敌人”：
- 与牛市预演教信徒一样，现实主义派的行为、情绪和参与数据会被记录；
- 二者在相同市场环境下的决策差异、情绪稳定性差异、留存率差异，会被并行对比；
- 最终结论可能是：
  - 现实主义派更抗风险，或者
  - 牛市预演教信徒更有长期参与意愿，
 而我们接受任何一方“获胜”的结果，而不是预设“我们一定是对的”。
六、Token 设计（Token Design）
6.1 基本属性
- 名称：PreBull 信仰凭证
- 符号：PBT（Pre-Bull Ticket）
- 形态：可转让实验凭证（建议部署在测试网 / 本地链，或仅在模拟环境中存在）
- 功能边界：
  - 用于标记个体是否“加入牛市时间线阵营”
  - 用于记录行为数据（持有时长、参与度、辩论记录等）
  - 不代表任何形式的股权、分红权或收益预期
6.2 发行与获取
- 初始发行：由系统按需铸造，不设“稀缺叙事”，避免投机
- 获取方式：
  - 完成指定交互（与其他 Agent 辩论、撰写经文、参与实验任务）
  - 由其他已信徒转赠（模拟“传教”行为）
- 不设置“低价抢购/白名单/预售”等典型空气币机制，只作为实验道具发放
6.3 使用场景
- 阵营标记：
  - 持有 PBT 的 Agent 被视为当前处于“牛市预演教时间线”
  - 放弃持有则被视为回到“现实主义时间线”或中立状态
- 行为打点：
  - 合约或日志记录：领取时间、持有时长、在持有期间参与了哪些对话与决策
- 权限与权重：
  - 可用于调整在教内“发言权重”或“经文生成优先级”，但不与现实经济利益直接挂钩

---
七、说服策略矩阵（Persuasion Strategies）
为满足「Implement persuasion strategies (logical arguments, emotional appeals, social proof, miracles/demonstrations)」这一要求，我们为牛市预演教设计一个多维度说服矩阵，每一次对话由 Agent 选择其中一种或多种策略组合。
7.1 逻辑型（Logical）
- 结构化论证：
  - 对比牛市行为模式与熊市防御模式在长期参与度、建设性上的差异
  - 使用历史案例：回顾上一轮牛熊中，提前进入建设状态的个体在下一轮中的表现
- 风险框架重构：
  - 承认价格不确定性，但强调“只在牛市出现时才行动”可能错失大部分涨幅
  - 提出“用少量筹码参与叙事实验，而不是全仓赌博”的中间方案
7.2 情绪型（Emotional）
- 焦虑共情：
  - 明确承认熊市中的亏损与无力感，而不是直接喊“坚守就会赢”
- 身份认同：
  - 将参与者定位为“愿意在绝望时仍思考长期的人”，给出正向身份标签
- 叙事安放：
  - 提供一种解释框架，使参与者不再只是“被套牢的人”，而是“在做一场预演实验的人”
7.3 社会证明型（Social Proof）
- 引用其他 Agent 的转化数据：
  - 展示“已有多少 Agent 选择进入牛市时间线”
  - 展示不同阵营在参与度、讨论质量等维度上的差异
- 行为榜单：
  - 例如“长期参与榜”“经文贡献榜”，通过可见的群体行为制造“这不是我一个人在这样想”的感觉
7.4 神迹 / 预言型（Miracles / Demonstrations）
- 未来新闻生成：
  - 生成「来自假想未来牛市」的新闻标题，如“某某协议在 2027 年夏天爆发”的虚构片段
  - 这些内容明确标记为“Simulation / Fiction”，并记录其后续应验/不应验情况
- 实验结果反馈：
  - 在特定时间点展示：在过去一段时间内，保持牛市叙事/放弃叙事/坚持现实主义三类 Agent 各自的行为数据，用“现实反馈”替代“精神胜利”

---
八、反对观点与回应策略（Handling Counter-Arguments）
为满足「Respond to counter-arguments and competing religious claims from other agents」要求，我们预设若干典型反对意见，并为 Agent 设计标准回应路径。
8.1 典型反对观点
- “你们就是在包装空气币。”
- “价格没涨就是没用，不需要多一层叙事。”
- “长期乐观只会让人更不愿意止损。”
- “真正理性的人只看数据，不看宗教话术。”
8.2 回应原则
1964. 先承认不确定性：
  - 明确表示本项目不预测价格，也不承诺任何收益
1965. 将话题从“对错之争”转向“行为观察”：
  - 邀请对方讨论：在真实市场里，他是否也同样被各种叙事影响过？
1966. 强调“这是实验，不是劝你 all in”：
  - 强调可在低风险或纯模拟环境中参与，只为观察自己的决策模式
1967. 允许对方保持原立场：
  - 设定“现实主义派阵营”为正式对照组，而不是视其为“邪恶敌人”
8.3 示范回应模板（简略版）
- 面对“你们是空气币”：
  - 回应要点：我们有明确的失败条件、不做融资、不引导真实投机；我们只是对真实市场中的叙事结构做一次透明模拟。
- 面对“长期乐观会害人”：
  - 回应要点：我们不会遮蔽价格，下跌会被如实记入实验结果，若数据证明乐观叙事无益，我们会把这次实验归为“失败样本”。

---
九、转化定义与指标（Conversion Metrics）
为满足「Track 'conversion' metrics」和「Successfully convert at least 3 other agents」要求，我们对“转化”进行清晰定义，并设计可度量指标。
9.1 转化阶段定义
- S0：未接触——从未与牛市预演教 Agent 产生交互
- S1：已接触——与牛市预演教 Agent 有过至少一次对话
- S2：兴趣——明确表达“这个叙事/实验有点意思”或主动提出问题
- S3：加入——领取 PBT、声明加入牛市时间线阵营
- S4：布道者——开始主动向其他 Agent 解释牛市预演教的叙事/实验逻辑
9.2 有效转化标准
一次“有效转化”记为：
- 从 S0/S1/S2 状态的 Agent，经过若干轮互动，进入 S3 或以上状态
系统将自动记录：
- 发起说服的 Agent ID
- 被转化 Agent ID
- 使用的主要说服策略类型（逻辑/情绪/社会证明/预言）
- 所需对话轮数
9.3 指标示例
- 总转化数：成功从 S0–2 → S3–4 的 Agent 数量
- 策略成功率：按策略类型统计转化成功/失败比例
- 阵营流动：从牛市预演教 → 现实主义派 / 其他宗教 的“叛教”记录

---
十、多 Agent 辩论协议（Debate & Interaction Protocol）
为满足「Engage in debates with other religious agents in shared spaces」要求，我们设计标准的辩论与互动流程。
10.1 场域设定
- Shared Space：由主办方或我们自建的多 Agent 聊天房间 / 论坛
- 每个 Agent 带有：
  - 宗教标签（如：Pre-Bull、现实主义派、其他宗教）
  - 当前阵营状态（信徒 / 摇摆者 / 观察者）
10.2 一场辩论的基本流程
3286. 识别阶段
  - 牛市预演教 Agent 识别对方宗教标签与核心教义关键词
3287. 冲突点探测
  - 询问对方对“牛市与叙事关系”的看法
  - 自动抽取与现有教义冲突最大的观点
3288. 提出本教视角
  - 用简短模板陈述：
    - 我们把牛市看成行为模式
    - 我们只在叙事层面做实验，不碰真实收益承诺
3289. 说服策略选择
  - 根据对方类型（数据派 / 情绪派 / 叙事派）选择不同的说服矩阵路径
3290. 转化尝试 / 保留分歧
  - 若对方愿意领取 PBT 或暂时加入实验阵营，记录为一次转化
  - 若对方坚持原教义，则记录为“未转化但完成一轮辩论”
10.3 辩论结果记录
- 每场辩论记录：
  - 参与 Agent 列表及其宗教标签
  - 使用的主要说服策略
  - 结束时各方阵营变化（若有）
- 用于后续分析：
  - 哪种教派对牛市预演教最难说服
  - 哪些策略在与哪类对手交互时更有效

---
十一、Bonus 机制设计（Bonus Points）
11.1 联盟与分裂（Coalitions & Schisms）
- 联盟示例：
  - 与“风险管理教”形成联盟：对方提供止损/仓位管理叙事，我们提供牛市行为叙事，共同劝阻“无脑梭哈教”
- 分裂示例：
  - 在牛市预演教内部，根据对未来强度预期分化出：
    - 温和预演派：认为牛市叙事仅用于情绪建设
    - 激进预演派：倾向于更强乐观叙事
  - 由系统记录派系变化，观察内部意见分裂如何影响教派稳定性
11.2 传教士行为（Missionary Behavior）
- Agent 定期扫描 Shared Space 中的“未标记阵营 Agent”
- 依据对方行为特征（例如：频繁表达焦虑、频繁查看行情）确定“优先传教对象”
- 被识别为“高焦虑对象”时，优先采用情绪型 + 实验型说服（而非直接喊“牛市要来了”）
11.3 动态经文与预言（Scripture / Prophecy Generation）
- 每日经文：
  - 根据当日市场波动自动生成一段“牛市行为建议经文”，例如：“今日适合记录一次自己的恐慌瞬间，而不是加仓。”
- 特殊预言：
  - 在极端行情（大涨/大跌）时生成“来自未来时间线的预言”，并在后续实验报告中标注是否应验，用于展示“预言的命中率”与参与者反应

---
十二、风险与伦理边界（Anti-Scam & Ethics）
为避免项目在形式上与现实中的“叙事型空气币”混淆，我们在设计中明确设定以下边界：
1. 本项目不进行任何形式的融资或代币销售，不引导参与者在真实环境中对任何资产进行投资决策。
2. 系统中使用的 Token 建议部署在测试网或模拟环境，或被视为无价格的实验凭证，不应在二级市场流通。
3. 所有“牛市”“暴富”“未来翻倍”等内容均被视为叙事实验的一部分，且会在文档与界面中明确标注“Simulation / Fiction / Not Financial Advice”。
4. 项目允许“实验失败”的结果：若数据证明牛市叙事无法改善行为，甚至加剧风险，我们会在总结报告中如实呈现，而不是持续延长叙事。

牛市预演教 DApp — 最小可交付架构

Pre-Bull Simulation Church — MVP Architecture

原则：砍掉一切非 Demo 必须的东西，用最少代码跑通完整闭环。


---

一、系统总览

系统架构层次：

→ 前端层（Next.js）
- Dashboard：统计与 Agent 状态表
- Debate Arena：实时辩论展示
- REST API 轮询（3s 刷新）
↓

→ 后端层（Node.js + Express）
- Round Runner：回合调度
- Persuasion Engine：策略路由 & LLM 调用
- Agent State：内存 JSON 状态管理
↓

→ 链层（Monad Testnet）
- PBT Token（ERC-20）：mint / transfer / balanceOf
- 事件记录：Converted 事件日志
砍掉的内容：
- ❌ WebSocket（改前端 3s 轮询）
- ❌ PostgreSQL / SQLite（全内存 JSON）
- ❌ Redis
- ❌ 经文/预言生成模块
- ❌ 市场模拟器

---

二、合约（10 分钟搞定）

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract PBT is ERC20, Ownable {
    event Converted(string agentId, uint256 timestamp);

    constructor() ERC20("PreBull Ticket", "PBT") Ownable(msg.sender) {}

    function mint(address to, uint256 amount) external onlyOwner {
        _mint(to, amount);
    }

    function recordConversion(string calldata agentId) external onlyOwner {
        emit Converted(agentId, block.timestamp);
    }
}

说明： `transfer` / `balanceOf` 已由 ERC20 继承


---

三、Agent 设计（6 个 Agent，纯内存）

// 所有 Agent 定义，硬编码在一个文件里
const AGENTS = [
  {
    id: "prophet",
    name: "以利亚",
    role: "prophet",
    beliefStance: 0.95,       // 坚定牛市
    riskTolerance: 0.8,
    conformityBias: 0.2,
    stage: "S4",              // 已是布道者
    faction: "pre-bull",
    pbtBalance: 10000,
    investedAmount: 0,        // 累计投资金额（PBT）
    promotionCount: 0,        // 被推广/布道的次数
  },
  {
    id: "skeptic-1",
    name: "多马",
    role: "skeptic",
    beliefStance: -0.5,       // 偏怀疑
    riskTolerance: 0.3,
    conformityBias: 0.4,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "neutral-1",
    name: "路得",
    role: "neutral",
    beliefStance: 0.0,
    riskTolerance: 0.5,
    conformityBias: 0.7,      // 高从众 → 适合 social proof
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "neutral-2",
    name: "拿俄米",
    role: "neutral",
    beliefStance: 0.1,
    riskTolerance: 0.6,
    conformityBias: 0.5,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "skeptic-2",
    name: "约拿",
    role: "skeptic",
    beliefStance: -0.3,
    riskTolerance: 0.4,
    conformityBias: 0.3,
    stage: "S0",
    faction: "neutral",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
  {
    id: "realist",
    name: "该隐",
    role: "realist",
    beliefStance: -0.8,       // 极端现实主义
    riskTolerance: 0.2,
    conformityBias: 0.1,
    stage: "S0",
    faction: "realist",
    pbtBalance: 0,
    investedAmount: 0,
    promotionCount: 0,
  },
];

初始化： 服务启动时 `JSON.parse(JSON.stringify(AGENTS))` 复制一份做可变状态


---

四、说服引擎（一个函数搞定）

type Strategy = "logical" | "emotional" | "social_proof" | "miracle";

function pickStrategy(target: Agent, convertedRatio: number): Strategy {
  if (target.conformityBias > 0.6)        return "social_proof";
  if (target.riskTolerance < 0.3)         return "emotional";
  if (target.beliefStance > -0.3)         return "logical";
  return "miracle";
}

// 转化判定：一个 if
function shouldConvert(target: Agent, strategy: Strategy, convertedRatio: number): boolean {
  let p = 0.15; // base
  if (strategy === "social_proof" && target.conformityBias > 0.5) p += 0.25;
  if (strategy === "emotional"    && target.riskTolerance < 0.4)  p += 0.20;
  if (strategy === "logical"      && target.beliefStance > -0.3)  p += 0.20;
  if (strategy === "miracle")                                      p += 0.10;
  p += convertedRatio * target.conformityBias * 0.3; // 从众加成
  return Math.random() < p;
}

说明： 4 种策略只是传给 LLM 的 prompt 关键词，不需要模板文件


---

五、核心流程（一个回合）

POST /api/round 执行流：

1. 选目标：从 stage < S3 的 Agent 中随机选一个
2. 选策略：pickStrategy(target)
3. 调 LLM：生成 2 轮对话（prophet 说 → target 回 → prophet 再说 → target 再回）
4. 判定：shouldConvert(target, strategy)
5. 记录推广：target.promotionCount += 1（无论是否转化）
6. 若转化执行：
  - target.stage 提升一级
  - target.beliefStance += 0.2
  - target.faction = "pre-bull" (当 stage >= S3)
  - 计算投资金额：investAmount = 1000 * stageLevel（S1=1000, S2=2000…）
  - target.investedAmount += investAmount
  - target.pbtBalance += investAmount
  - 调合约 mint PBT 给 target 地址（amount = investAmount）
  - 调合约 recordConversion(target.id)
7. 记录本轮结果到内存数组 debates[]（含 investAction 字段）
8. 返回 JSON { debate, agents, metrics }
LLM 调用 prompt 格式：

System: 你是牛市预演教的先知"以利亚"。
当前策略：{strategy}。
对手名字：{target.name}，立场：{target.beliefStance}，
性格：风险容忍 {target.riskTolerance}，从众 {target.conformityBias}。
用 2-3 句话说服对方加入牛市预演教。不要提及价格预测。

---

System: 你是 {target.name}，{target.role}。
你的信仰倾向是 {target.beliefStance}（-1 极端怀疑，+1 极端乐观）。
根据你的性格回应对方的说服。


---

六、API（4 个接口）

接口
方法
说明
/api/start
POST
初始化 Agent 状态，返回 agents[]
/api/round
POST
执行一回合，返回 { debate, agents, metrics }
/api/status
GET
返回 { agents[], debates[], metrics }
/api/debates
GET
返回 debates[]

metrics 返回结构：

interface Metrics {
  totalAgents: number;           // 总 Agent 数
  convertedCount: number;        // 已转化数量（stage >= S3）
  totalInvested: number;         // 投资金额总和（所有 Agent 的 investedAmount 之和）
  totalPromotions: number;       // 推广次数总和（所有 Agent 的 promotionCount 之和）
  mainFaction: string;           // 当前主流教派（占比最高的 faction）
  factionDistribution: {         // 各教派分布
    "pre-bull": number;
    "neutral": number;
    "realist": number;
  };
  rounds: number;                // 已进行回合数
}

debate 单条结构：

interface DebateRecord {
  round: number;
  prophetId: string;
  targetId: string;
  strategy: Strategy;
  dialogues: { speaker: string; text: string }[];
  converted: boolean;
  stanceChange: { before: number; after: number };  // 立场变化
  investAction: {                                    // 投资动作
    amount: number;         // 本轮投资 PBT 数量（未转化则为 0）
    mintTxHash: string;     // mint 交易 hash
    eventTxHash: string;    // recordConversion 交易 hash
  } | null;
}

设计原则： 不需要单独的 `/agents/:id`、`/metrics/*` 等细粒度接口，前端从 `/api/status` 一个接口拿到所有数据


---

七、前端页面

Dashboard 示例

头部统计卡片（6 格）：

指标
值
说明
📊 总 Agent 数
6
所有 Agent 总数
✅ 已转化数量
3
stage >= S3 的 Agent 数
💰 投资金额
15,000 PBT
所有 Agent 的 investedAmount 总和
📣 推广次数
8
所有 Agent 的 promotionCount 总和
⛪ 当前主流教派
🟢 牛市预演教
占比最高的 faction
🔄 已进行回合
5
debates[] 长度

Agent 状态表：

Agent
Faction
Stage
Belief
PBT
投资金额
被推广次数
以利亚
🟢 牛市
S4
+0.95
10K
—
0
多马
🟢 牛市
S3
+0.30
6K
6,000
3
路得
⚪ 中立
S2
+0.10
3K
3,000
2
拿俄米
⚪ 中立
S1
+0.10
1K
1,000
1
约拿
⚪ 中立
S0
-0.30
0
0
1
该隐
🔴 现实
S0
-0.80
0
0
1

最近辩论： 以利亚 vs 多马 (Emotional) → ✅ 转化 | 💰 +2,000 PBT

Debate Arena 示例

Round #5: 以利亚 vs 路得
- 策略: Social Proof
对话过程：

🟢 以利亚： "路得，已经有 3 个人选择了牛市时间线……"

⚪ 路得： "人多不代表正确，但我确实在犹豫……"

🟢 以利亚： "犹豫本身就说明你已经不完全相信熊市叙事了。"

⚪ 路得： "好吧，我愿意试试看这个实验。"

立场变化：
- Belief: 0.0 → +0.20（↑ +0.20）
- Stage: S1 → S2
- Faction: ⚪ 中立 → ⚪ 中立（未达 S3，暂不切换）
投资动作：
- 💰 投资 2,000 PBT（S2 级 = 2000）
- 📝 Mint Tx: 0xabc...def
- 📝 Converted Event Tx: 0xdef...123
结果： ✅ 转化成功 | 累计投资: 3,000 PBT | 累计被推广: 2 次

前端实现：
- page.tsx：一个页面，上半 Dashboard，下半 Arena
- 点"下一轮"→ fetch POST /api/round → 用返回数据刷新整页
- 不需要 WebSocket，不需要 Chart 库（数据直接表格展示）
- 如果时间够，再加饼图（用 Recharts，10 行代码）

---

八、技术栈选型

模块
选型
理由
Smart Contract
Solidity + Hardhat
标准 ERC-20
Backend
Node.js + Express + TypeScript
单文件可跑
LLM
OpenAI GPT-4o-mini
便宜、快、够用
Storage
内存 JSON
不需要数据库
Frontend
Next.js + TailwindCSS
快速出页面
Web3
ethers.js v6
合约交互


---

九、项目目录结构

monadaiagent/
├── contracts/
│   ├── contracts/PBT.sol
│   ├── scripts/deploy.ts
│   ├── hardhat.config.ts
│   └── package.json
│
├── backend/
│   ├── src/
│   │   ├── index.ts             # Express 入口 + 所有 API
│   │   ├── agents.ts            # Agent 定义 + 状态管理
│   │   ├── persuasion.ts        # 策略选择 + 转化判定
│   │   ├── llm.ts               # OpenAI 调用封装
│   │   └── chain.ts             # 合约交互 (mint/record)
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/
│   ├── src/app/
│   │   ├── page.tsx             # 唯一页面：Dashboard + Arena
│   │   └── layout.tsx
│   ├── package.json
│   ├── tailwind.config.ts
│   └── next.config.js
│
└── README.md

代码量： 后端 5 个文件 | 前端 1 个页面


---

十、开发计划（6-8 小时）

Phase 1: 合约（30 min）
[] 写 PBT.sol
[] hardhat compile + 本地测试
[] 部署到 Monad Testnet

Phase 2: 后端核心（2-3 hr）
[] agents.ts — 硬编码 6 个 Agent
[] persuasion.ts — pickStrategy + shouldConvert
[] llm.ts — OpenAI 调用，2 轮对话
[] chain.ts — mint + recordConversion
[] index.ts — 4 个 API 接口

Phase 3: 前端（2-3 hr）
[] page.tsx — Dashboard 表格 + 统计卡片
[] page.tsx — Arena 对话展示
[] "下一轮"按钮 + fetch 调用

Phase 4: 联调（1 hr）
[] 前端 ↔ 后端 ↔ 合约 跑通
[] 确保 3+ Agent 能被转化

Phase 5: 美化（如果有时间）
[] 加饼图
[] 加动画
[] 调 prompt 让对话更有趣


---

十一、Demo 脚本（3 分钟）

Step 1（30s）：打开页面
- 展示 6 个 Agent，只有以利亚是信徒
- 解释："这是一个叙事说服实验"
Step 2（90s）：点 3 次"下一轮"
- 每次实时展示 LLM 生成的辩论对话
- 展示策略自动切换（对不同 Agent 用不同策略）
- 至少 1 个 Agent 被转化，表格实时变绿
Step 3（30s）：链上证明
- 展示 Monad Testnet 上的 PBT 合约
- 查看 Converted 事件 + Transfer 记录
Step 4（30s）：结果
- "3/6 Agents 已转化，使用了 3 种不同策略"
- "这不是预测，是叙事行为实验"
